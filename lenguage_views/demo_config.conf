# Ejemplo de config para Hyprland
monitor=monitor=eDP-1,1920x1080@60,0x0,1

input {
    kb_layout = es
    follow_mouse = 1
}

# Un poco de bash para rematar
alias v="nvim"
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi
